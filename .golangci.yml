version: "2"

linters:
  default: none
  enable:
    - errcheck
    - staticcheck
    - govet
    - unused
    - ineffassign
    - bodyclose
    - gocritic
  settings:
    govet:
      enable:
        - shadow
  exclusions:
    paths:
      - vendor
