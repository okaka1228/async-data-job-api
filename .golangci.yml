version: "2"

linters:
  default: none
  enable:
    - errcheck
    - staticcheck
    - govet
    - unused
    - ineffassign
    - typecheck
    - bodyclose
    - gocritic
  exclusions:
    paths:
      - vendor

linters-settings:
  govet:
    enable:
      - shadow
