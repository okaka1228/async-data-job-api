linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - staticcheck
    - unused
    - ineffassign
    - typecheck
    - bodyclose
    - gocritic

linters-settings:
  govet:
    enable:
      - shadow

issues:
  exclude-dirs:
    - vendor
